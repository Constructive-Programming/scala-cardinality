# The report lists every definition the project's Compile sources introduce, with the cardinality
# each holds, and writes the same report to the file the build asks for.
> cardinalityReport
$ must-mirror report.txt expected/report.txt
